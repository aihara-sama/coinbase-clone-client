import React, { FunctionComponent, HTMLAttributes } from 'react';

interface IProps {
  type?: 'big' | 'medium' | 'small';
}

const Input: FunctionComponent<
  IProps & HTMLAttributes<HTMLInputElement>
> = () => {
  // ~~~~~ Redux state ~~~~~

  // ~~~~~ Hooks ~~~~~

  // ~~~~~ Cmp state ~~~~~

  // ~~~~~ Refs ~~~~~

  // ~~~~~ Effects ~~~~~

  // ~~~~~ Handlers ~~~~~

  return null;
};

export default Input;
