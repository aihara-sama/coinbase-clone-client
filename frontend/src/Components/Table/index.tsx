import React, { FunctionComponent } from 'react';

interface IProps {
  columns: string[];
  rows: FunctionComponent[];
}

const Table: FunctionComponent<IProps> = () => {
  // ~~~~~ Redux state ~~~~~

  // ~~~~~ Hooks ~~~~~

  // ~~~~~ Cmp state ~~~~~

  // ~~~~~ Refs ~~~~~

  // ~~~~~ Effects ~~~~~

  // ~~~~~ Handlers ~~~~~

  return <div></div>;
};

export default Table;
