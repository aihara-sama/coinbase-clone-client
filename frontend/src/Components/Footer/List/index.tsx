import React, { FunctionComponent } from 'react';

interface IItem {
  href: string;
  text: string;
}

interface IProps {
  title: string;
  items: IItem[];
}

const List: FunctionComponent<IProps> = () => {
  // ~~~~~ Redux state ~~~~~

  // ~~~~~ Hooks ~~~~~

  // ~~~~~ Cmp state ~~~~~

  // ~~~~~ Refs ~~~~~

  // ~~~~~ Effects ~~~~~

  // ~~~~~ Handlers ~~~~~

  return <div></div>;
};

export default List;
