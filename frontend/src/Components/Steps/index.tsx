import React, { FunctionComponent } from 'react';

interface Step {
  text: string;
  icon: FunctionComponent;
}

interface IProps {
  steps: Step[];
}

const Steps: FunctionComponent<IProps> = () => {
  // ~~~~~ Redux state ~~~~~

  // ~~~~~ Hooks ~~~~~

  // ~~~~~ Cmp state ~~~~~

  // ~~~~~ Refs ~~~~~

  // ~~~~~ Effects ~~~~~

  // ~~~~~ Handlers ~~~~~

  return <div></div>;
};

export default Steps;
