import React, { FunctionComponent } from 'react';

interface ILink {
  href: string;
  text: string;
}

interface IProps {
  title: string;
  description: string;
  link?: ILink;
}

const Card: FunctionComponent<IProps> = () => {
  // ~~~~~ Redux state ~~~~~

  // ~~~~~ Hooks ~~~~~

  // ~~~~~ Cmp state ~~~~~

  // ~~~~~ Refs ~~~~~

  // ~~~~~ Effects ~~~~~

  // ~~~~~ Handlers ~~~~~

  return <div></div>;
};

export default Card;
