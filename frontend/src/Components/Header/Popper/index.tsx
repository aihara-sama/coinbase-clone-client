import React, { FunctionComponent } from 'react';

interface IProps {
  children: FunctionComponent | FunctionComponent[];
}

const Popper: FunctionComponent<IProps> = () => {
  // ~~~~~ Redux state ~~~~~

  // ~~~~~ Hooks ~~~~~

  // ~~~~~ Cmp state ~~~~~

  // ~~~~~ Refs ~~~~~

  // ~~~~~ Effects ~~~~~

  // ~~~~~ Handlers ~~~~~

  return <div></div>;
};

export default Popper;
